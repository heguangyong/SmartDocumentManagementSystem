spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/test_minio?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: redis
      port: 6379
      password: R@yw51asy9a!
      database: 10
      jedis:
        pool:
          max-active: 800
          max-wait: 10000
          max-idle: 8
          min-idle: 0

  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB

file:
  upload-dir: uploads/
  share:
    default-expire-millis: 7200000
    secret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

oauth:
  api_url: https://passport.library.sh.cn/
  client_id: 1000855101
  client_secret: bf5a28d7ff094b6ec91cea1b655bf1863e02c2daa140598fcd198bcb0f0349fb
  redirect_uri: https://driver.library.sh.cn/api/user/authup
  scope: read

myset:
  nowsvr: up
  download_url_1: https://shlib.cn/#/download
  upload_url_2: http://shlib.cn/#/upload
  bucket_max_mb: 5120
  timeout_front_day: 7
  timeout_back_day: 30

minio:
  endpoint: http://host.docker.internal:9000
  access-key: admin
  secret-key: admin123
  bucket-name: sdmsfilesmanager

aws:
  credentials:
    accessKey: admin
    secretKey: admin123
    region:
      static: us-east-1
    stack:
      auto: false
  s3:
    endpoint: http://host.docker.internal:9000
    bucket:
      name: sdmsfilesmanager

logging:
  level:
    org.springframework.security: DEBUG
    com.github.sdms.controller.OnlyOfficeController: DEBUG
    com.github.sdms.controller.FileController: DEBUG

svs:
  service:
    enabled: false
    addrs: 60.247.61.98:15009
    timeout: 20000
    name: default
    verify:
      cert-flag: true
      allow-list-flag: true
      chain-flag: true
      valid-flag: true
      crl-flag: true

onlyoffice:
  server:
    url: http://onlyoffice:80
  callback:
    timeout: 30000
  upload:
    max-size: 100MB

app:
  external-base-url: http://sdms-backend:8080